DATABASE_URL="postgresql://rosterduel:rosterduel@localhost:5432/rosterduel?schema=public"
REDIS_URL="redis://localhost:6379"
PORT=4000

# Required only for live post-game recap generation (spec section 4b —
# src/recap/anthropicRecapGenerator.ts). Not needed to run the app, the
# tests, or the demo — recap unit tests use a fake generator instead.
# Get a key at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=
